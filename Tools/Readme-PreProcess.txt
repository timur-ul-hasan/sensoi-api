Run in Command Prompt from Source code path

Usage:
preprocess.py [-h] -i INPUTDIR -o OUTPUTDIR

Example:
python preprocess.py -i "D:\Medical Project\TextProcess\TextFile" -o "D:\Medical Project\TextProcess\PreprocessFile"

Details:
Remove Stop words and apply Stemmer and Lemma filter to text 

